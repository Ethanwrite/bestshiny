# Dashboard

The V1 operational API is complete. A separate frontend can consume `/v1/generations`, `/v1/accounts`,
`/v1/workers`, `/v1/providers` and generation event histories without importing provider-specific concepts.

